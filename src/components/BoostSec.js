import React from 'react';

// Styles
import styles from './boostSec.module.scss'

const BoostSec = () => {
    return (
        <section className={styles.container} >
            <h2>Boost your links today</h2>
            <button>Get started</button>
        </section>
    );
};

export default BoostSec;